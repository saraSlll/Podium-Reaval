
export type StageConfig = {
  third: { color: string; icon: string };
  second: { color: string; icon: string };
  first: { color: string; icon: string };
};

export type Winner = {
  place: number;
  name: string;
  score: number;
};
